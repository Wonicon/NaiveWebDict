环境要求：mysql驱动
	  mysql内含database naive2
	  naive2中含有4张table:count,dict,user,user_like	  
	  count 内含结构为
	  {
	  	char[20] word;//被点赞的单词
	  	int dict_id;//来源的代号
	  	int count;//被点赞次数 
	  }
	  
	  dict内含结构为
	  {
	  	int dict_id;//来源代号
	  	char[30] dict_name;//来源名字
	  }
	  
	  user内含结构为
	  {
	  	char[20] username;
	  	char[20] password;
	  	bool login;
	  }
	  
	  user_like内含结构为
	  {
	  	char[20] username;
	  	char[20] word;
	  	int dict_id;
	  }
	 	
java类内含7个函数
boolean login(String username,String password)//用于修改sql中user的login值（登录）
void logout(String username,String password)//用于修改sql中user的login值（注销）
boolean register(String username,String password)//用于添加sql中user的数据（注册）
boolean like_word(String username,String like_word,int source)//用于添加sql中user_like的数据和修改count的数据（点赞）
boolean dislike_word(String username,String like_word,int source)//用于删除sql中user_like的数据和修改count的数据（取消点赞）
ResultSet query_count(String like_word)//返回sql中count的统计数据（每个词在每个来源的点赞次数）
boolean query_user(String like_word,int source,String username)//返回sql中user_like的数据（用户是否在某个来源点赞）

其中ResultSet query_count(String like_word)中返回的ResultSet中存放的是一张表，
表的结构
{
	int dict_id;
	int count;
}并且存放的时候根据dict_id进行升序排序